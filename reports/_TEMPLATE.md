# {ID} · {论文英文标题}

> **一句话**：这篇论文做了什么、解决了什么问题、在 GFlowNet × OT 地图上站在哪个位置。（2–3 句，直接下判断，不加免责声明）

| 字段 | 内容 |
|---|---|
| arXiv | [{arxiv_id}](https://arxiv.org/abs/{arxiv_id}) |
| 发表 | {venue}（主会 / Workshop / 预印本 必须分清） |
| 作者 | {作者列表} |
| 代码 | {链接 或 「未公开」} |
| 本仓库 PDF | `papers/{arxiv_id}.pdf` · 中译 `papers_zh/{arxiv_id}.zh.pdf` |
| 阅读优先级 | P0 / P1 / P2 + 一句理由 |

## 1. 问题设定与记号

沿用原文记号。给出：状态空间/图结构、源与汇、流 \(F\)、策略 \(P_F, P_B\)、奖励 \(R\)、目标分布等（按论文实际内容取舍）。指出与标准 DAG-GFlowNet 设定的差异（是否允许环、是否固定 \(P_B\)、边缘约束等）。

## 2. 核心贡献（按原文编号）

逐条列出，每条标注原文位置（Theorem X / Prop. Y / Sec. Z / Eq. (n)），一条一段，先说结论再说前提。

## 3. 方法与理论推导要点

把最关键的 2–4 个公式或推导链写出来（LaTeX，`\(...\)` 行内、`\[...\]` 行间），每个公式注明原文出处。解释每一步「为什么成立」而不是复述符号。若有算法伪代码，用要点概括训练循环。

## 4. 实验与证据

表格列出主要实验：任务 / 基线 / 指标 / 关键数字（数字必须来自原文，标注 Table/Figure 编号）。说明证据支撑到什么程度：哪些结论是实验直接支持的，哪些是作者推断的。

## 5. 前提假设与适用边界

列出结论依赖的假设（有限性、吸收性、无环性、cost 结构、边缘固定等）。写成正向的适用范围，而不是「本文不声称……」。

## 6. 在 GFlowNet × OT 主线中的位置

- 前驱：它依赖哪些工作（给本仓库编号）
- 后继/对照：谁在它之上、谁与它竞争
- 对「内部流选择 = 最优传输」这条主线贡献了什么（或为什么是必要先修）

## 7. 可复用的 insight 与开放问题

3–6 条。每条要具体到可以变成一个实验或一条定理草稿。

## 8. 引用

```bibtex
@article{...}
```
