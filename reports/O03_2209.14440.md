# O03 · GeONet: a neural operator for learning the Wasserstein geodesic

> **一句话**：GeONet 把「给定一对边缘分布、求 Wasserstein 测地线」这件事从"每来一对就重解一次 OT"改写成一次性训练的算子学习：用两组 DeepONet 分别拟合 Benamou–Brenier 问题的原始–对偶 KKT 方程（连续性方程 + Hamilton–Jacobi），训练只需要边界分布对、不需要测地线真值，推理是一次前向传播。在 GFlowNet × OT 地图上它站在「摊销 / amortization」这一侧：它证明了动态 OT 的最优性条件可以被一个跨分布族的算子吃下来，而这正是「条件 GFlowNet–OT」想要的能力——只不过 GeONet 的实现机制（PDE 残差）在离散图上不存在对应物。

| 字段 | 内容 |
|---|---|
| arXiv | [2209.14440](https://arxiv.org/abs/2209.14440)（v4, 2024-05-23） |
| 发表 | **UAI 2024 主会**（原文首页脚注：Accepted for the 40th Conference on Uncertainty in Artificial Intelligence） |
| 作者 | Andrew Gracyk（UIUC 统计系）、Xiaohui Chen（USC 数学系） |
| 代码 | https://github.com/agracyk2/GeONet （原文 Sec. 4 给出） |
| 本仓库 PDF | `papers/2209.14440.pdf` · 中译 未生成 |
| 阅读优先级 | **P2** —— 方法本体与 GFlowNet 没有共享结构，但它是「摊销式 OT 求解器」这条支线里最早把*整条动态轨道*而非静态映射作为输出的工作，作为对照系必须读 |

## 1. 问题设定与记号

沿用原文记号。空间域 \(\Omega \subset \mathbb{R}^d\)，两端分布 \(\mu_0,\mu_1 \in \mathcal{P}_2(\Omega)\)（绝对连续），代价固定为二次代价 \(c(x,y)=\|x-y\|_2^2\)（Sec. 2.1）。目标是 \((\mathcal{P}_2(\Omega),W_2)\) 中连接两端的常速测地线 \(\{\mu_t\}_{t\in[0,1]}\)，它是该度量空间中的绝对连续曲线，记作 \(AC(\mathcal{P}_2(\Omega))\)。

原文要学的对象是一个**算子**（Eq. (12)–(13)）：

\[
\Gamma^\dagger:\ \mathcal{P}_2(\Omega)\times\mathcal{P}_2(\Omega)\to AC(\mathcal{P}_2(\Omega)),\qquad (\mu_0,\mu_1)\mapsto \{\mu_t\}_{t\in[0,1]} .
\]

动态形式是 Benamou–Brenier 问题（Eq. (4)）：在连续性方程 \(\partial_t\mu+\mathrm{div}(\mu v)=0\) 与两端约束 \(\mu(\cdot,0)=\mu_0,\ \mu(\cdot,1)=\mu_1\) 下最小化动能 \(\int_0^1\int \tfrac12\|v\|_2^2\mu\,dx\,dt\)。对偶变量 \(u(x,t)\) 是连续性方程的 Lagrange 乘子（Eq. (5)），最优速度场是 \(v^\ast=\nabla u^\ast\)，\(u^\ast\) 满足 Hamilton–Jacobi 方程 \(\partial_t u+\tfrac12\|\nabla u\|_2^2=0\)（Eq. (6)）。原文还指出 \(u^\ast(\cdot,1)=\psi^\ast\)、\(u^\ast(\cdot,0)=-\varphi^\ast\)（差一个常数），即 HJ 解就是静态 Kantorovich 势的时间插值（Sec. 2.1 末）。

与标准 DAG-GFlowNet 设定的差异，逐条对照：

- **两端都固定**。GeONet 固定 \(\mu_0\) 与 \(\mu_1\)（Eq. (4) 的两个边界条件）；标准 GFlowNet 只固定终端 reward \(R(x)/Z\)，源是单点 \(s_0\)。只有把 GFlowNet 的初始流分布也钉死（本仓库 O08 的做法），两者的约束结构才对齐。
- **无图、无组合动作**。GeONet 的"状态"是 \(\mathbb{R}^d\) 中的点、"转移"是 ODE 流，自动微分可以直接算 \(\partial_t,\ \mathrm{div},\ \nabla\)；图上没有 \(\nabla u\)，HJ 方程的离散对应物是 Bellman/最短路方程，不是 PDE 残差。
- **无环性不成问题**。时间参数 \(t\in[0,1]\) 天然给出全序，环根本无法出现；这正是 GFlowNet 非无环理论（T19/T36）要费力处理的地方。
- **cost 固定**。二次代价写死在 HJ 方程里（Eq. (6) 的 \(\tfrac12\|\nabla u\|^2\) 就是 \(\tfrac12\|\cdot\|^2\) 的 Legendre 共轭）；换代价要重推 PDE。

## 2. 核心贡献（按原文编号）

**(C1) 把测地线求解重写为算子学习问题（Sec. 3, Eq. (12)–(13)）。** 结论：一次训练之后，对新的 \((\mu_0,\mu_1)\) 只需一次前向传播即可给出整条 \(\{\mu_t\}\)。前提：训练分布族与测试分布族同构（原文 OOD 实验显示分布外误差上升 2–3 倍，见 Sec. 4）。

**(C2) 训练不需要测地线真值（Sec. 1「Surprisingly...」段 + Eq. (19)）。** 结论：监督信号只有边界对 \((\mu_0^{(i)},\mu_1^{(i)})\)，中间时刻靠 PDE 残差把 KKT 条件"焊"住。这是它相对纯数据驱动 neural operator 的核心便宜之处，也是它误差在 \(t=0.25/0.5/0.75\) 明显大于 \(t=0,1\) 的原因（原文 Appendix H.1 自己承认）。

**(C3) 原始与对偶两套网络联合训练，用零对偶间隙做正确性锚（Sec. 2.1 + Appendix B, Eq. (32)）。** 结论：只要 \((\mu^\ast,u^\ast)\) 同时满足 CE 与 HJ 并满足边界条件，对偶间隙为零，解就是测地线。这条是 GeONet 全部设计的支点。

**(C4) 输出网格无关，支持 zero-shot 超分辨率（Sec. 1, Fig. 1；Table 2 的 high-res. 行）。** 结论：在 \(24\times24\) 上训练、在 \(75\times75\) 上取值，误差与同分辨率随机测试基本持平（2D high-res. \(6.29\%\text{–}7.88\%\) vs 2D random \(6.33\%\text{–}7.13\%\)，Table 2）。

**(C5) 熵正则版 ER-GeONet（Appendix D, Eq. (56)–(58)）。** 结论：把约束改成 \(\partial_t\mu+\mathrm{div}(\mu v)+\varepsilon\Delta\mu=0\)，KKT 变成 \(\partial_t\mu+\mathrm{div}(\mu\nabla u)=-\varepsilon\Delta\mu\)、\(\partial_t u+\tfrac12\|\nabla u\|_2^2=\varepsilon\Delta u\)。后者是抛物型方程、有唯一光滑解，训练更稳；\(\varepsilon\downarrow0\) 时按消失粘性方法收敛到 Benamou–Brenier 解（引 Mikami 2004、Evans 2010）。**注意这条是 Appendix，正文实验没有报 ER-GeONet 的数字。**

**(C6) 梯度增强（Appendix E, Eq. (61)–(62)）。** 把 PDE 对空间坐标再求一次导、把新残差也加进损失。原文说法是提高样本效率与精度，未给出增强前后的对照数字。

## 3. 方法与理论推导要点

**(1) KKT 条件是怎么来的（Appendix B, Eq. (20)–(32)）。** 推导链只有三步，每步都靠"尺度化到 \(\pm\infty\)"这个技巧：

\[
\min_{(\mu,v)\in(\mathrm{CE})}\int_0^1\!\!\int \tfrac12\|v\|_2^2\mu \;=\; \min_{(\mu,v)}\max_u L(\mu,v,u)\;\geqslant\;\max_u\min_{(\mu,v)} L(\mu,v,u).
\]

第一步等号成立是因为：若 \(\mu\notin(\mathrm{CE})\)，把 \(u\) 乘一个任意大的常数就让内层 \(\max\) 爆到 \(+\infty\)（Eq. (25)），所以 \(\max\) 自动把违反连续性方程的候选剔除。第二步是弱对偶。第三步：分部积分后对 \(v\) 逐点求极小得 \(v^\ast=\nabla u\)，代回得 Eq. (28)；此时如果 \(\partial_t u+\tfrac12\|\nabla u\|_2^2\neq0\)，因为 \(\mu\) 除边界外不受约束，同样的尺度化让内层 \(\min\) 掉到 \(-\infty\)（Eq. (29)）。于是对偶间隙非负、且等于零当且仅当

\[
\partial_t\mu+\mathrm{div}(\mu\nabla u)=0,\qquad \partial_t u+\tfrac12\|\nabla u\|_2^2=0,\qquad \mu(\cdot,0)=\mu_0,\ \mu(\cdot,1)=\mu_1 .
\]

这就是原文 Eq. (7) = Eq. (32)。**为什么值得关心**：它把一个带约束的变分问题换成了一组可以逐点检查的方程，于是"最优性"变成了可以用自动微分算残差的东西——这是 GeONet 能不用真值训练的全部原因。

**(2) 双分支 DeepONet 参数化（Eq. (14)–(15)）。** 原始网络与对偶网络各用两个 branch（分别编码 \(\mu_0,\mu_1\) 的 \(m\) 点离散值）乘一个 trunk（吃 \((x,t)\)）：

\[
C(\mu_0,\mu_1)(x,t;\phi)=\sum_{k=1}^{p} B_k^{0,\mathrm{cty}}(\mu_0;\theta^{0,\mathrm{cty}})\,B_k^{1,\mathrm{cty}}(\mu_1;\theta^{1,\mathrm{cty}})\,T_k^{\mathrm{cty}}(x,t;\xi^{\mathrm{cty}}),
\]

\(H(\mu_0,\mu_1)(x,t;\psi)\) 同构（Eq. (15)）。\(C_\phi\) 近似测地线密度，\(H_\psi\) 近似 HJ 势。因为 trunk 吃的是连续坐标 \((x,t)\)，输出天然网格无关；branch 吃的是固定 \(m\) 维离散向量，所以**输入侧并不网格无关**——原文 Sec. 4.6 把这列为限制之一。

**(3) 损失（Eq. (16)–(19)）。** \(\mathcal{L}_{\mathrm{cty}}\)、\(\mathcal{L}_{\mathrm{HJ}}\) 是两条 PDE 的 \(L^2\) 残差（配权 \(\alpha_1,\alpha_2\)），\(\mathcal{L}_{\mathrm{BC}}\) 是两端边界残差（配权 \(\beta_0,\beta_1\)）；Algorithm 1 的循环是：均匀采 \(N\) 个 collocation 点 \((x,t)\)、算两条残差、算边界残差、加权求和、一步优化器。实验取 \(\alpha_1=0.5,\alpha_2=0.25,\beta_0=\beta_1=1\)（Sec. 4.1，边界项权重更大是刻意的）。

**(4) 二阶导的工程细节（Sec. 4.1「Training」）。** 直接对 DeepONet 的内积结果做自动微分可行；显式算 Hessian 内存开销过大、无法用大 batch，原文明确说这条路"not viable"。这决定了 \(\mathcal{L}_{\mathrm{HJ}}\) 只能以 \(\|\nabla H_\psi\|^2\) 的一阶形式出现。

**(5) 两条损失会不会互相打架？** 原文专门查了这件事（Appendix H.3, Fig. 8）：在编码 MNIST 实验的训练后期观察 CE 损失与 HJ 损失的振荡，结论是**没有观察到对抗性竞争**，且 CE 损失的数值远大于 HJ 损失。这条负面结果对 GFlowNet 侧有直接意义——它说明"原始约束（流守恒）"和"对偶约束（势的最优性）"在同一个优化器里可以共存，不需要交替优化；对照 O04，那里的 DSBM 恰恰因为必须交替优化两个损失而被批评（O04 Sec. 1）。

**(6) 架构与超参的敏感度（Sec. 4.1）。** 输出维度 \(p\) 从 200 提到 800，1D 训练损失从 \(\approx2.5\times10^{-4}\) 降到 \(\approx1.5\times10^{-4}\)、测试误差降约 1 个百分点；2D 从 \(p=400\) 到 \(p=800\)，损失从 \(\approx2.1\times10^{-5}\) 到 \(\approx1.8\times10^{-5}\)。branch 宽度 100→150 使 1D 训练损失降约 \(4\times10^{-5}\)，再加宽收益 \(\approx1\times10^{-5}\)。结论：**宽度早早饱和，输出维度 \(p\) 更值钱**——这与"branch/trunk 的乘积形式相当于一组基函数展开、\(p\) 就是基的个数"的解释一致。

## 4. 实验与证据

| 任务 | 基线 | 指标 | 关键数字（原文出处） |
|---|---|---|---|
| 1D 高斯混合（\(k_0=k_1=6\)，20,000 训练对，\(\Omega=[0,10]\) 100 点网格） | POT 参考解 | 相对 \(L^1\) 误差（%） | identity \(2.67\text{–}3.04\)；random \(4.65\text{–}5.76\)；high-res.(200 点) \(4.76\text{–}6.01\)；OOD \(13.8\text{–}22.2\)（Table 2） |
| 2D 高斯混合（12 个分量，5,000 训练对，\(24\times24\) 输入） | POT | 相对 \(L^1\)（%） | identity \(6.42\text{–}7.70\)；random \(6.33\text{–}7.13\)；high-res.(\(75\times75\)) \(6.29\text{–}7.88\)；OOD \(9.67\text{–}10.2\)（Table 2） |
| 2D 点云（经验分布，3,000 粒子/3 分量混合） | CFM (Tong et al. 2023)、Rectified Flow (Liu et al. 2023)，POT 当真值 | 相对 \(L^1\)（%） | \(t=0.5\)：GeONet \(30.0\pm1.10\)、CFM \(98.9\pm2.41\)、RF \(112\pm3.61\)；\(t=0\)：CFM/RF 均为 \(0.0\)（Table 3） |
| 编码 MNIST（预训练 AE 压到 \(\mathbb{R}^{32}\)，30,000 对） | POT | 相对 \(L^1\)（%） | encoded random \(1.54\text{–}2.78\)；ambient random \(24.7\text{–}68.1\)（Table 6） |
| 替代指标 | — | \(L^2\) / \(W_1\)（2D 用 sliced） | 1D random：\(L^2\ 4.81\text{–}7.28\)，\(W_1\ 0.347\text{–}0.403\)；2D random：\(L^2\ 6.87\text{–}8.11\)，sliced-\(W\ 0.245\text{–}0.275\)（Table 7，均 \(\times10^{-2}\)） |
| 运行时 | POT（1D 停止阈值 0.5，2D 阈值 10.0；30 对，1D 20 个时间步、2D 5 个） | wall-clock | 原文只给 log-log 图（Fig. 6），文字结论是"speeds computation up to orders of magnitude"，**没有给具体秒数表** |

训练规模（Table 4/5）：1D/2D 各约 2 小时、经验高斯 2 小时、编码 MNIST 约 4 小时，单张 NVIDIA T4；最终训练损失分别约 \(1.5\times10^{-4}\)、\(1.8\times10^{-5}\)、\(7.0\times10^{-4}\)、\(2.0\times10^{-2}\)。

证据强度分级：

- **实验直接支持**：网格无关性（high-res. 行与 random 行同量级）；在点云设定下比 CFM/RF 更接近 POT 测地线（Table 3 差 3–4 倍）；推理比 POT 快若干数量级（Fig. 6 的趋势）。
- **作者推断、证据偏弱**：「physics-informed DeepONet 随维度升高而失效」——依据只是"2D 方差变小时误差快速上升、1D 不明显"这条观察（Appendix H.1），没有 \(d\geqslant3\) 的定量实验（Fig. 9 只有 3D 高斯的可视化，无误差表）。
- **有反证**：本仓库 O05（UNOT, ICML 2025）Fig. 8 直接对比，声称其 McCann 插值比 GeONet 更接近真值，且 UNOT 既没在测地线上训练也没见过 MNIST。GeONet 自己也承认"tends to smooth, or regularize, the solutions"（Sec. 4.2）。
- **数字口径要注意**：Table 3 里 CFM/RF 在 \(t=0\) 是 \(0.0\)，因为初始数据对它们是直接给定并直接学的（原文 Sec. 4.2 明说），这一列不构成 GeONet 的劣势。

## 5. 前提假设与适用边界

适用范围写成正向：

1. **二次代价、绝对连续两端分布、凸（或紧）域 \(\Omega\)**。KKT 推导用到 \(\|x\|_2\to\infty\) 时的衰减条件做分部积分（Appendix B）；常速测地线的存在性用的是 Brenier 映射 + McCann 插值（Appendix C, Thm. C.12, Eq. (52)）。
2. **branch 输入维度固定且为预设网格**。原文 Sec. 4.6 第二条限制：GeONet 要求 branch 的取值点事先定好，训练对必须同分辨率；输出侧才是网格无关的。
3. **空间维度不高**。branch 输入规模随 \(d\) 指数增长（Sec. 4.6 第一条限制）；论文给出的缓解手段是先用自编码器降维（MNIST 实验）。
4. **同分布或近分布的测试对**。OOD 设定（1D 均值范围从 \([2,8]\) 扩到 \([1,9]\)、分量数 6→8；2D 分量数 12→16，均值范围外扩）下相对误差上升约 10 个百分点（Sec. 4.5 + Table 2）。
5. **测地线可被光滑函数表示**。MNIST 原像空间近似分段常值/跳跃不连续，physics-informed 学习直接失效，必须先编码（Sec. 4.3）；ambient 空间误差比 encoded 空间高一个数量级以上（Table 6）。
6. **没有泛化误差界**。原文 Sec. 4.6 第三条明确把"给出 GeONet 预测风险的泛化界"列为 future work。

## 6. 在 GFlowNet × OT 主线中的位置

- **前驱**：O01（OT for Machine Learners，Benamou–Brenier 与 Kantorovich 对偶的标准参考）；数值参考实现依赖 POT（Flamary et al. 2021）。方法论前驱是 DeepONet（Lu et al. 2021）、physics-informed DeepONet（Wang et al. 2021b）与 enhanced DeepONet（Tan & Chen 2022）。
- **后继 / 对照**：
  - **O05（UNOT, ICML 2025）** 是它的直接竞争者，且给出了不利于 GeONet 的对照（O05 Sec. 4.3, Fig. 8）。两者摊销的对象不同：GeONet 摊销"整条测地线"，UNOT 摊销"熵 OT 对偶势"，后者再由势导出计划、重心、测地线。
  - **O06（Q-flow, AISTATS 2025）** 与它解同一个 Benamou–Brenier 问题，但走神经 ODE + 双向 KL 松弛，是 per-pair 求解、不摊销。
  - **O04（α-DSBM, NeurIPS 2024）** 解熵正则的动态版本（Schrödinger 桥），同样 per-pair。GeONet 的 ER-GeONet（Appendix D）与 O04 在目标上重叠，但 GeONet 用 PDE 残差、O04 用迭代投影 + bridge matching。
- **对主线（"内部流选择 = 最优传输"）贡献了什么**：GeONet 提供了这条主线的**连续侧对偶模板**。O08 的 Theorem 3.2 说：min-flow 非无环 GFlowNet 在固定初始边流分布后等价于以图最短路为代价的 Kantorovich 问题（也等价于离散 Beckmann）；O08 的 Theorem 3.3 给出其对偶 LP，并说明最优流只能支撑在最短路子图上。把这两条与 GeONet 的 KKT 对照，结构是一一对应的：
  - 连续性方程 \(\partial_t\mu+\mathrm{div}(\mu\nabla u)=0\) ↔ 图上流守恒（O08 Eq. (4)）；
  - HJ 方程 \(\partial_t u+\tfrac12\|\nabla u\|^2=0\) ↔ 图上最短路/Bellman 方程，其解 \(d(u,x)\) 正是 O08 里的 ground cost；
  - 零对偶间隙 ↔ O08 证明中的 \(\mathrm{GFlow}^\star \leqslant \mathrm{OT}^\star\) 与反向不等式夹逼。
  换句话说：**GeONet 学的 \(H_\psi\) 就是 GFlowNet 里"状态流的对数/势函数"的连续版本**。这个对应关系是把 GeONet 的摊销技巧搬到 GFlowNet 上的唯一可靠接口——不是搬 PDE 残差，而是搬"用对偶量做无真值监督"这个想法。

## 7. 可复用的 insight 与开放问题

1. **「用最优性条件替代真值」可以直接移植成 GFlowNet 的辅助损失。** GeONet 的 \(\mathcal{L}_{\mathrm{HJ}}\) 在图上的对应物是"势函数满足 \(\pi_x - \pi_u \leqslant d(u,x)\) 且在支撑边上取等"（O08 Theorem 3.3 的对偶可行性 + 互补松弛）。可写成一条实验：在 min-flow GFlowNet 训练里加一项对偶互补松弛残差，看是否加速收敛到 OT⋆ 而不需要更长的轨迹采样。
2. **摊销的正确切面是"对偶势"而不是"轨道"。** GeONet 输出整条 \(\{\mu_t\}\) 的代价是中间时刻误差显著大于两端（Table 2 每一行都是中间高、两端低）；UNOT 只摊销势、其余靠一步 Sinkhorn 补，反而更准。对条件 GFlowNet–OT 的启示：条件网络应该预测 \(\log F\) 或 \(P_B\) 这类"势型"量，而不是直接预测整条轨迹分布。
3. **网格无关 ≠ 输入无关**，这个坑值得在图上重演一次。GeONet 的 trunk 无关而 branch 有关；GFlowNet 若要跨图规模泛化，编码器必须对节点数不变（例如图神经网络 + 位置编码），否则会重复 GeONet 的"输入侧被钉死"限制。
4. **中间时刻缺监督是 physics-informed 摊销的结构性弱点。** 一条定理草稿：在只有两端约束 + PDE 残差的训练目标下，中间时刻的误差下界与 collocation 采样密度和 PDE 残差算子的条件数有关；对应到 GFlowNet，就是"只用 TB 损失（两端）而不用 DB/SubTB（中间）时的 credit assignment 误差"（可与 T03/T05 的结论对照）。
5. **熵正则是共享的稳定化旋钮。** ER-GeONet 的 \(\varepsilon\Delta u\) 让 HJ 变抛物、解唯一光滑（Appendix D）；O04 的 \(\varepsilon\) 是布朗运动方差；GFlowNet 里对应的是"策略熵/温度"。开放问题：在 min-flow GFlowNet 里显式加熵项，是否得到图上的熵 OT（即 Sinkhorn 计划），以及其 \(\varepsilon\downarrow0\) 极限是否恢复最短路支撑（O08 Theorem 3.3）。
6. **OOD 退化幅度可以当作条件化能力的量尺。** GeONet OOD 误差从 \(\sim5\%\) 涨到 \(\sim20\%\)（1D，Table 2）。评估"条件 GFlowNet–OT 是否真有跨任务泛化优势"时，应该照抄这个协议：训练分布参数区间 → 测试时外扩区间 → 报告误差涨幅，而不是只报同分布指标。

## 8. 引用

```bibtex
@inproceedings{gracyk2024geonet,
  title     = {GeONet: a neural operator for learning the Wasserstein geodesic},
  author    = {Gracyk, Andrew and Chen, Xiaohui},
  booktitle = {Proceedings of the 40th Conference on Uncertainty in Artificial Intelligence (UAI)},
  year      = {2024},
  note      = {arXiv:2209.14440v4},
  url       = {https://arxiv.org/abs/2209.14440}
}
```

---

**编者注（本报告作者自行决定的事项）**

1. 阅读优先级沿用 `data/papers.yaml` 中已登记的 P2，理由自拟（方法本体与 GFlowNet 无共享结构，但作为摊销支线的对照系必要）。
2. 发表状态按原文首页脚注判定为 UAI 2024 **主会**（非 workshop）；arXiv 版本以本仓库 PDF 的 v4（2024-05-23）为准。
3. Table 2/3/6/7 原文数值单位为"\(\times10^{-2}\)"，本报告统一换算成百分号表述；每行只取该行的最小–最大值范围以控制篇幅，逐时刻明细见原文。
4. 第 6 节把 GeONet 的 KKT 与 O08 的 Theorem 3.2/3.3 做结构对照，这个映射是本报告的判断，原文没有任何 GFlowNet 相关讨论。
5. 运行时加速倍数原文只给了 log-log 图与"orders of magnitude"的文字表述，未给数值表，故本报告不编造具体倍数。
