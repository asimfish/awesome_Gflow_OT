# O06 · Computing high-dimensional optimal transport by flow neural networks

> **一句话**：Q-flow 用一个神经 ODE 直接解 Benamou–Brenier 动态 OT：两端分布 \(P,Q\) 都只有有限样本、都没有解析密度，于是把两个终端约束松弛成 KL（用 logistic 分类器现场估计密度比），再加一个有限差分形式的 \(W_2\) 传输代价项，双向交替训练。训练出的 OT 轨道被直接用于两个下游任务：图像到图像翻译与高维密度比估计（DRE）。**先把最容易的误读挡掉：标题里的 "flow neural network" 指的是连续正规化流 / 神经 ODE 的速度场，与 GFlowNet 无任何关系**（详见第 1 节末）。

| 字段 | 内容 |
|---|---|
| arXiv | [2305.11857](https://arxiv.org/abs/2305.11857)（v5, 2025-03-10） |
| 发表 | **AISTATS 2025 主会**（本仓库 `data/papers.yaml` 登记）；本仓库 PDF 为纯 arXiv v5，**页面上没有会议页脚**，故发表状态未能由 PDF 自身证实 |
| 作者 | Chen Xu（Georgia Tech, ISyE）、Xiuyuan Cheng（Duke 数学系）、Yao Xie（Georgia Tech, ISyE） |
| 代码 | https://github.com/hamrel-cxu/FlowOT （原文 Sec. 5） |
| 本仓库 PDF | `papers/2305.11857.pdf` · 中译 未生成 |
| 阅读优先级 | **P2** —— 与 GFlowNet 无结构共享，但它是"每对分布单独求解动态 OT"这一端的干净参照物（与 O03/O05 的摊销端正好相反），且它的 KL-松弛技巧对 GFN–OT 的边缘约束处理有直接可迁移性 |

## 1. 问题设定与记号

沿用原文记号。目标是找可逆的连续时间传输映射 \(T_t:\mathbb{R}^d\to\mathbb{R}^d\)，\(T_0=\mathrm{Id}\)、\((T_1)_\#P=Q\)，并令 \(\rho(x,t):=(T_t)_\#p\) 满足 \(\rho(\cdot,0)=p,\ \rho(\cdot,1)=q\)。\(T_t\) 由神经 ODE 表示（Eq. (1)）：

\[
\dot x(t)=f(x(t),t;\theta),\quad x(0)\sim p,\qquad T_s^t(x)=x(s)+\int_s^t f(x(t'),t';\theta)\,\mathrm{d}t' \ \ (\text{Eq. }(5)).
\]

动态 OT 即 Benamou–Brenier（Eq. (2)）：

\[
\inf_{\rho,v}\ \mathcal{T}:=\int_0^1\mathbb{E}_{x(t)\sim\rho(\cdot,t)}\|v(x(t),t)\|^2\mathrm{d}t\quad \text{s.t.}\quad \partial_t\rho+\nabla\!\cdot\!(\rho v)=0,\ \rho(\cdot,0)=p,\ \rho(\cdot,1)=q .
\]

在 \(p,q\) 的正则性条件下 \(\min\mathcal{T}=W_2^2(p,q)\)，最优 \(v\) 是该传输问题的最优控制。

数据设定是本文与 O03/O05 的最大差别：只给两组 i.i.d. 样本 \(X=\{X_i\}_{i=1}^N\sim P\)、\(\tilde X=\{\tilde X_j\}_{j=1}^M\sim Q\)，**两端都没有解析密度**，所以 CNF 的换元法算 KL 那一套不可用（Sec. 3.1 明说）。

与标准 DAG-GFlowNet 设定的差异，以及**"flow" 一词的歧义澄清**：

- **本文的 "flow" 是流体力学意义的流**：一个速度场 \(v(x,t)\) 加上连续性方程；"flow neural network" 是 CNF（continuous normalizing flow）传统下的命名，作者自己解释 "Q-flow" 是照 CNF 命名习惯取的（Sec. 1）。**GFlowNet 的 "flow" 是有向图上边/状态的未归一化测度 \(F(s\to s')\)，其守恒律是对父/子节点求和（O08 Eq. (4)），没有速度场、没有 PDE、也没有时间坐标。**
- 混淆并非全无来由：两者都叫 flow、都有守恒律。但对象类型不同（\(\mathbb{R}^d\) 上的密度 vs 图上的测度）、守恒律形式不同（PDE vs 求和恒等式）、监督信号不同（样本 vs 未归一化 reward \(R(x)\) 与未知 \(Z\)）。本文全文**没有** reward、没有配分函数、没有 \(P_F/P_B\)。
- 另两处设定差异：本文两端都固定且时间区间固定为 \([0,1]\)；GFlowNet 的源是单点 \(s_0\)、轨迹长度在非无环设定下无界。

## 2. 核心贡献（按原文编号）

**(C1) Q-flow net：从两组样本直接学连续可逆传输映射，并用传输代价把它推向动态 OT（Sec. 3, Eq. (3)–(8)）。** 结论：端到端训练可以"精修"任何初始流（两个 CNF 拼接、或分布插值网络给出的流），使其从"只匹配终端"变成"匹配终端且传输代价小"。前提：初始流已经近似匹配两端（Sec. 3.3），否则分类器难训。

**(C2) 双向训练（Eq. (3) + Remark 1）。** 结论：Benamou–Brenier 对 \(P,Q\) 是对称的——把 \(P\) 放 \(t=0\) 或放 \(t=1\)，最优解是同一个速度场、只差时间方向。因此同时优化 \(L^{P\to Q}\) 与 \(L^{Q\to P}\) 在最优处指向同一个解，有限样本下可提升精度。**这条有实验支撑**（Table A.3：BPD 从单向 1.08/1.19/1.31 改进到双向 1.05/1.14/1.31）。

**(C3) flow-ratio net：用 OT 轨道做无穷小 DRE（Sec. 4, Eq. (10)–(12)）。** 结论：由 \(\log(q/p)=\int_0^1\partial_t\log p(x,t)\mathrm{d}t\)（Eq. (A.2)），把"时间 score" \(\partial_t\log p(x,t)\) 参数化为 \(r(x,t;\theta_r)\)，在相邻时间格点上做 logistic 分类训练；训好后任意 \(s<t\) 的 \(\log(p(x,t)/p(x,s))\) 都可积分得到。**关键论点是"用 OT 轨道当桥比用线性插值桥更好"**，因为后者（Eq. (A.8) 的 \(X(t_k)=\sqrt{1-\alpha_k^2}X(0)+\alpha_kX(1)\)，TRE/DRE-\(\infty\) 用的方案）与 OT 轨道明显不同（Fig. A.9 vs Fig. 3(a)）。

**(C4) 实验上同时打过静态与动态 OT 基线，以及 SB 基线（Sec. 5）。** 在 Korotin 的 OT benchmark、CelebA64 对齐、图像翻译、高维 MI、MNIST 能量模型上给出数字（见第 4 节）。

## 3. 方法与理论推导要点

**(1) 终端约束怎么变成可算的损失（Eq. (6)–(7)）。** 两端都没有密度，于是训一个 logistic 分类网络 \(c_1(x;\varphi_c)\)：

\[
\min_{\varphi_c}\ \frac1N\sum_{i=1}^N\log\!\big(1+e^{c_1(T_0^1(X_i;\theta);\varphi_c)}\big)+\frac1M\sum_{j=1}^M\log\!\big(1+e^{-c_1(\tilde X_j;\varphi_c)}\big).
\]

**为什么成立**：该损失总体版本的函数最优解是 \(c_1^\ast=\log(q/p_1)\)（原文"by direct computation"），于是 \(\mathrm{KL}(p_1\|q)=-\mathbb{E}_{x\sim p_1}c_1^\ast(x)\)，有限样本形式即 Eq. (7) 的 \(L_{\mathrm{KL}}^{P\to Q}(\theta)=-\frac1N\sum_i c_1(T_0^1(X_i;\theta);\hat\varphi_c)\)。这与 GAN 判别器同构，但用途不同：**这里判别器不是对手，而是一个"KL 读数器"**。代价是内层必须频繁重训——原文强调每训几步 \(\theta\) 就要更新 \(c_1,\tilde c_0\)，否则密度比估计失效（Sec. 3.2「Inner-loop training」，Algorithm 1 第 5、10 行）。

**(2) 传输代价怎么变成有限差分（Eq. (8)）。** 取时间网格 \(0=t_0<\dots<t_K=1\)、\(h_k=t_k-t_{k-1}\)、\(X_i(t;\theta):=T_0^t(X_i;\theta)\)：

\[
L_T^{P\to Q}(\theta)=\frac1N\sum_{i=1}^N\sum_{k=1}^K\frac{\|X_i(t_k;\theta)-X_i(t_{k-1};\theta)\|^2}{h_k}.
\]

其总体形式在最优处可解释为离散时间的 \(\sum_k W_2^2(\rho(\cdot,t_{k-1}),\rho(\cdot,t_k))/h_k\)（引 Xu et al. 2022）。**工程上这一项免费**：\(X_i(t_k)\) 在做 ODE 积分时已经算出来了（Appendix C.1 明说 \(W_2\) 损失不增加函数求值次数）。它同时起到"保证数值可逆性"的作用——Table A.2 报的逆映射误差 \(\mathbb{E}_{x\sim P}\|T_1^0(T_0^1(x))-x\|_2^2+\mathbb{E}_{y\sim Q}\|T_0^1(T_1^0(y))-y\|_2^2\) 为 \(7.24\times10^{-7}\)（moon→checkerboard）、\(3.44\times10^{-5}\)（\(d=320\) 高斯）、\(5.23\times10^{-5}\)（MNIST）。

**(3) 完整目标与 \(\gamma\) 的角色（Eq. (3)–(4)）。** \(\min_\theta(L^{P\to Q}+L^{Q\to P})\)，其中 \(L^{P\to Q}=L_{\mathrm{KL}}^{P\to Q}+\gamma L_T^{P\to Q}\)。**注意 \(\gamma\) 的方向**：原文写"with small \(\gamma\) the terminal condition is enforced"——\(\gamma\) 小则更重视端点匹配、轨道可能不够光滑；\(\gamma\) 大则传输代价压得狠、但推过去的分布与目标的 KL 更大（Appendix B.4 的解释）。实验取 \(\gamma=0.1\)（高维高斯）、\(1\)（CelebA64）、\(0.05/0.1\)（图像翻译）、\(0.5\)（DRE 系列）。

**(4) 训练循环（Algorithm 1）与积分细节。** 外层交替：先跑 \(E\) 个 epoch 的 \(P\to Q\) 精修（每步后更新 \(c_1\) 若干 \(E_{\mathrm{in}}\) 个 epoch），再跑 \(Q\to P\)。ODE 积分把每个 \([t_{k-1},t_k]\) 再切成 \(S\)（实验用 3–5）个小段、每段四阶 Runge–Kutta，目的是保证数值精度与可逆性。计算复杂度（Appendix C.1）：主导项是 \(O(8KSE(M+N))\) 次 \(f\) 求值，分类器额外 \(O(4(E_0+EE_{\mathrm{in}})(M+N))\) 次。

**(5) 初始流从哪来（Sec. 3.3 + Appendix C.2）。** 两条路：(i) 拼接两个 CNF（各自在 \(P\leftrightarrow Z\)、\(Z\leftrightarrow Q\) 之间，\(Z=\mathcal{N}(0,I_d)\)，用 JKO-iFlow / Xu et al. 2023）；(ii) 分布插值网络（Interflow / Albergo–Vanden-Eijnden 2023），或直接用线性插值 Eq. (A.8) 造桥再匹配。Sec. 5.1–5.2 用 (ii)，Sec. 5.3 用 (i)。预训练很轻：Table 1a 的例子约 7 分钟（占 Q-flow 训练时间 9%）、Table 1b 约 18 分钟（12%）。

**(6) 一个容易被忽略的副产品：用 Q-flow 反过来改进预训练生成模型（Appendix B.3.3）。** MNIST 能量模型实验里，每个预训练模型给出可逆映射 \(F\) 使 \(Q=F_\#\mathcal{N}(0,I_d)\)；作者在 \((F^{-1})_\#P\) 与 \((F^{-1})_\#Q=\mathcal{N}(0,I_d)\) 之间训 Q-flow，于是密度估计为 \(\log\hat p(x)=\log q(x)-\int_0^1 r(x,s;\hat\theta_r)\mathrm{d}s\)（\(\log q\) 由 \(F\) 的换元公式给出）。**同时得到一个改进的生成模型** \(\tilde Q:=(F\circ T_1^0)_\#\mathcal{N}(0,I_d)\)（Fig. A.7/A.8 展示改进后的数字）。这说明"学到的 OT 映射"本身就是一个可插拔的生成模型修正器——对 GFN–OT 的类比是：学到的最优耦合可以用来修正一个已有的采样器，而不只是用来报告传输代价。

**(7) 二维定性检查是本文唯一直观展示机制的地方（Sec. 5.3.1）。** two-moon → checkerboard：线性插值桥（Eq. (A.8)，Fig. A.9）与 Q-flow 学出的轨道（Fig. 3(a)）在视觉上明显不同，flow-ratio 网络估出的相邻时刻 \(\log p(x,t_k)-\log p(x,t_{k-1})\)（Fig. 3(b)）与真实密度变化吻合。高斯混合的 2D 对照里，DRE-\(\infty\) 倾向在 \(Q\) 的支撑上高估 \(r(x)\)，TRE 与单一 ratio 在 \(P\) 的支撑上低估，而本方法 MAE 最小（Fig. A.4，**只报 MAE 在图注里、无独立表**）。

## 4. 实验与证据

| 任务 | 基线 | 指标 | 关键数字（出处） |
|---|---|---|---|
| OT benchmark：高维高斯混合，\(d=64/128/256\)（\(P\) 为 3 分量、\(Q\) 由两个 10 分量混合的映射平均构造） | OTCFM、MMv1、MMv2、W2（后四者数字抄自 Korotin et al. 2021b） | (L\(_2\)-UVP ↓, cos ↑)，\(2^{14}\) 个样本 | Q-flow **(4.00, 0.98) / (2.12, 0.99) / (1.97, 0.99)**；OTCFM (4.64,0.97)/(2.78,0.99)/(3.02,0.98)；MMv1 (8.1,0.97)/(2.2,0.99)/(2.6,0.99)；MMv2 (10.1,0.96)/(3.2,0.99)/(2.7,0.99)；W2 (7.2,0.97)/(2.0,1.00)/(2.7,1.00)（Table 1a） |
| CelebA64 对齐（\(P_{\mathrm{Ckpt}}\) 为 WGAN-QC 三个检查点生成的人脸，\(Q\) 为真人脸） | NOT、OTCFM、MM、MM:R | (L\(_2\)-UVP ↓, cos ↑) | Q-flow **(0.87,0.99) / (0.27,0.97) / (0.12,0.97)**（Early/Mid/Late）；NOT (0.99,0.99)/(0.34,0.96)/(0.12,0.96)；OTCFM (1.2,0.99)/(0.39,0.96)/(0.16,0.95)（Table 1b） |
| 图像翻译（VAE 隐空间，latent \((12,8,8)\)，\(d=768\)；handbags 137K / shoes 50K；CelebA male 90K / female 110K；9:1 划分） | OTCFM、Re-flow、SBCFM、**DSBM**、W2GN、MM:R、DiscoGAN、CycleGAN、NOT | FID ↓（测试集） | Handbag→shoes：Q-flow **12.34**、NOT 13.77、OTCFM 15.96、CycleGAN 16.00、SBCFM 17.22、DiscoGAN 22.42、DSBM 25.71、Re-flow 25.92、MM:R 33.04、W2GN 34.23。CelebA male→female：Q-flow **9.66**、OTCFM 9.76、SBCFM 11.32、MM:R 12.34、NOT 13.23、W2GN 15.23、CycleGAN 17.74、Re-flow 20.24、DSBM 25.82、DiscoGAN 35.64（Table 2） |
| MNIST 能量模型 DRE（\(d=784\)） | DRE-\(\infty\)、TRE、单一 ratio | BPD ↓ | RQ-NSF：**1.05** vs 1.09/1.09/1.09；Copula：**1.14** vs 1.21/1.24/1.33；Gaussian：**1.31** vs 1.33/1.39/1.96（Table 3） |
| 高维 MI 估计，\(d\in\{40,80,160,320\}\)（\(P=\mathcal{N}(0,\Sigma)\) 块对角、off-diagonal 0.8；\(Q=\mathcal{N}(0,I_d)\)） | DRE-\(\infty\)、TRE、单一 ratio | 估计 MI vs 真值 | 与真值吻合、与 DRE-\(\infty\) 基本相同，优于另两个（Fig. A.6，**仅曲线无表**） |
| 消融：双向 vs 单向 | — | BPD ↓ | 双向 1.05/1.14/1.31；单向 1.08/1.19/1.31（Table A.3） |
| 消融：\(\gamma\times\) 时间网格 | — | BPD ↓ | \(\gamma\in\{0.5,1,5\}\) 与三种网格的 9 种组合，BPD 落在 **1.041–1.062**，最差仍优于最好基线 1.09（Table A.4） |
| 训练时间（单张 A100） | — | 小时 | Table 1a：Q-flow 1.25、OTCFM 1.25、MMv2 1.25、W2 1.25、MMv1 2.5；Table 1b：Q-flow 2.5、OTCFM 2.5、NOT 2.75、MM 2、MM:R 1.75（Table A.1，均不含初始流预训练） |
| DRE 训练成本 | DRE-\(\infty\) | 小时 | MNIST 上约 **8 小时 vs 33 小时**（Sec. 5.3.3） |

证据强度分级：

- **实验直接支持**：在 Korotin benchmark 与 CelebA64 上 L\(_2\)-UVP 全面持平或更低（Table 1a/1b）；两个图像翻译任务 FID 均为最低（Table 2），且是在**同一 VAE 隐空间、同样 mini-batch 数、同样模型大小**下重训所有可比基线（Sec. 5.2 明说 OTCFM/Re-flow/SBCFM/DSBM/W2GN/MM:R 都是自己重训的，只有 DiscoGAN/CycleGAN/NOT 的数字抄自 Korotin et al. 2023）；DRE 三个设定 BPD 均最优且超参不敏感（Table 3 + A.4）。
- **作者推断、未被独立验证**：核心机制主张"用 OT 轨道当 DRE 的桥优于线性插值桥"。支持它的是 BPD 全面更低 + 2D 定性图（Fig. 3 vs Fig. A.9），但**没有做"同一 flow-ratio 网络 + 换桥"的直接消融**（只做了双向/单向消融）。所以"是 OT 轨道带来的收益，还是 flow-ratio 网络本身带来的收益"未被分离。
- **理论空白（原文自陈，Sec. 6）**：终端约束用 KL 松弛，**没有一致性分析**，尤其在有限样本下；也没有"学到的确实是 OT 轨道"的理论保证。这与 O03（有 KKT 作锚）、O04（有收敛定理）形成鲜明对比——**O06 是这四篇里理论最薄的一篇**。
- **DSBM 在此处表现最差之一（FID 25.71 / 25.82，Table 2）**，与 O04 自己在 AFHQ 上的克制结论方向一致：SB 类方法在图像翻译的对齐度上不占优。但要注意这是在 VAE 隐空间、固定预算下重训的结果，与 O04 原文的像素空间设定不可直接对比。

## 5. 前提假设与适用边界

1. **两端只需有限样本，不需解析密度**——这是本文最宽松、也最有价值的假设（对比 TrajectoryNet 要求参考分布是高斯，Sec. 1.1）。
2. **需要一个已近似匹配两端的初始流**（Sec. 3.3）；端到端训练被明确定位为"refinement"。
3. **需要能反复重训内层分类器**，且更新要足够频繁（Sec. 3.2）；这决定了方法的实际成本结构。
4. **时间网格与 ODE 子步数是精度/成本的显式旋钮**（\(K\) 与 \(S\)，Appendix C.1 的 \(O(8KSE(M+N))\)）；数值可逆性依赖细网格。
5. **\(\gamma>0\) 需要调**，方向是"小 \(\gamma\) 保端点、大 \(\gamma\) 压代价"；Table A.4 显示在 MNIST DRE 上 \(\gamma\in[0.5,5]\) 内结论稳健。
6. **适用于 \(W_2\)（二次代价）**（Sec. 1.1 明说"we focus on the Wasserstein-2 distance, which suffices for many applications"）。
7. **没有 OT 最优性的理论保证**，只有传输代价被显式最小化这一事实；正确性完全由实验承担。

## 6. 在 GFlowNet × OT 主线中的位置

- **前驱**：Benamou–Brenier；CNF / 神经 ODE（Chen et al. 2018；Kobyzev et al. 2020）；带传输正则的 CNF（Finlay et al. 2020；Onken et al. 2021；Xu et al. 2022, 2023 的 JKO-iFlow）；分布插值 / Flow Matching（Albergo–Vanden-Eijnden 2023；Lipman et al. 2023；Liu 2022）；DRE 侧是 TRE（Rhodes et al. 2020）与 DRE-\(\infty\)（Choi et al. 2022）。
- **对照 / 竞争**：与 **O03（GeONet）** 解同一个 Benamou–Brenier 问题但走反方向（O03 摊销 + PDE 残差，O06 单对求解 + KL 松弛 + 采样）；与 **O04（α-DSBM）** 竞争同一批图像翻译任务，且在本文的 Table 2 里 DSBM 落后明显；与 **O05（UNOT）** 完全不同层次——O05 摊销静态熵 OT 的对偶势，O06 不摊销、解动态 OT。
- **对主线（"内部流选择 = 最优传输"）贡献了什么**：它贡献的是**约束处理技术**，而不是流选择机制。具体三点：
  1. **"把硬边缘约束换成可微 KL 读数"这一招在 GFN–OT 上直接可用**。O08 的构造需要固定初始边流分布 \(P_F(\cdot|s_0)=L\)（O08 Sec. 3.3 所说的 leward matching）与 reward matching \(P_B(x|s_f)=R(x)/Z\)。这两个都是硬约束，实践中靠损失项软化。O06 的做法给出一个更强的版本：用一个二分类器估计"当前推过去的分布"与"目标分布"的密度比，从而得到 KL 的无偏读数——在图上就是"用一个判别器区分 GFlowNet 采出的终止态与目标 reward 采样"，可作为 reward matching 违反度的一个可微代理。
  2. **传输代价项的有限差分形式（Eq. (8)）是"期望轨迹长度"的连续对应物**。O07/O08 用 \(\mathbb{E}[n_\tau]\) 或总流量作为代价；O06 用 \(\sum_k\|X(t_k)-X(t_{k-1})\|^2/h_k\)。两者都是"沿轨迹累加局部移动量"，且都**免费**（前者是轨迹长度、后者是已算出的中间点）。这条对应关系说明：GFlowNet 的流正则化项在连续设定下就是动能项，反之亦然。
  3. **双向训练的对称性论证（Remark 1）在图上未必成立**，值得单独查。Benamou–Brenier 的时间对称性来自 \(P,Q\) 地位对等；GFlowNet 的图是有向的、\(s_0\) 与 \(s_f\) 地位不对等（O07 Assumption 2.1），所以"前向训练与后向训练指向同一解"这个结论**不能照搬**。这是一个具体的、可以判真假的问题。
- **它不属于主线的原因（必须说清）**：本文的 "flow neural network" 与 GFlowNet 同名不同物（第 1 节已澄清）。把 O06 归到 GFN–OT 的相关工作里，只能以"动态 OT 的神经解法"的身份，不能以"flow network"的字面相似性为由。

## 7. 可复用的 insight 与开放问题

1. **用判别器读 KL，替代 GFN–OT 里的硬边缘约束。** 实验草案：在 O08 的 hypergrid 环境上，把 leward/reward matching 的平方损失换成"判别器估计的 KL"（Eq. (6)–(7) 的离散版），比较收敛速度与最终 \(\mathrm{OT}^\star\) 差距。风险点已被原文标出：内层判别器必须频繁重训，否则读数失效。
2. **一条定理草稿：KL-松弛的一致性。** 原文 Sec. 6 把"KL 处理边界条件的一致性分析"列为开放问题。在**有限图**上这个问题反而更容易：状态空间有限、密度比是有限维向量，可以直接给出"判别器估计误差 → KL 估计误差 → 最终耦合的传输代价误差"的传播界。**做出来同时补上 O06 的理论空白与 GFN–OT 的约束松弛理论。**
3. **"OT 轨道 vs 线性插值桥"这个消融必须补，而且在图上是廉价的。** O06 没做（第 4 节已指出）。图上可做：固定 GFlowNet 的下游任务（例如用中间状态分布做课程学习或 DRE），分别用 (i) 最小流量 GFlowNet 的轨道、(ii) 随机游走轨道、(iii) 均匀插值轨道当桥，看下游指标差异。这直接检验"最优轨道是否真的更有用"这个 GFN–OT 的隐含卖点。
4. **可逆性作为诊断量值得引入。** Table A.2 的逆映射误差（\(10^{-7}\sim10^{-5}\)）是一个便宜且信息量大的健康检查。GFlowNet 的对应量是"\(P_F\) 与 \(P_B\) 诱导的轨迹分布之差"（O08 Eq. (1) 的违反度），建议在训练日志里当作一等公民打印。
5. **时间方向对称性在有向图上是否成立，是一个可判定的小问题。** Remark 1 的论证依赖 \(P,Q\) 对等；图上 \(s_0\to s_f\) 有向。可构造反例或给出成立条件（例如图是可逆/双向的、或状态空间上存在使转移核可逆的参考测度）。
6. **摊销 vs 单对求解的成本交点。** O06 单对训练 1.25–2.5 小时（Table A.1），O05 一次性训练约 35 小时。因此"摊销划算"的阈值大约在 15–30 个任务量级。**这是把 O05 与 O06 的数字直接相除得到的粗估**（本报告的计算，非原文结论），但它给 GFN–OT 的摊销主张定了一个必须跨过的门槛。
7. **一个尚未有人做的组合**：用 O06 的判别器 KL 读数处理边缘约束、用 O08 的最小总流量选流、用 O05 的算子网络做跨任务摊销。三者的约束/代价/摊销分工互不冲突，是一条干净的组合路线。
8. **"精修已有流"这个范式本身可以搬到 GFlowNet。** O06 的定位不是从零学 OT，而是把任意初始流（两个 CNF 拼接 / 插值网络）**精修**成 OT 流（Sec. 3.2 的 "refinement"），预训练只占总时间的 9–12%（Appendix B.1）。GFN–OT 的对应做法：先用标准 TB 训一个只满足 reward matching 的 GFlowNet（不管流量大小），再加上流量正则做第二阶段精修，而不是一开始就带着正则项训。这与 O04 的两阶段（bridge matching 预训练 + \(\alpha\)-DSBM 微调）结构一致，**两篇不同路线的论文都收敛到"先匹配端点、再优化代价"这个顺序，值得当作一条经验规律采纳并验证**。
9. **副产品视角：最优耦合可以当采样器修正器。** 见第 3 节 (6)。GFN–OT 里的对应实验：用学到的最优耦合把一个已有的（次优的）终止态采样器修正到目标 reward 分布，报告修正前后的 TV 距离——O08 已经在用 TV 作指标（其 Table 1），所以这个实验的评估管线是现成的。

## 8. 引用

```bibtex
@inproceedings{xu2025flowot,
  title     = {Computing high-dimensional optimal transport by flow neural networks},
  author    = {Xu, Chen and Cheng, Xiuyuan and Xie, Yao},
  booktitle = {Proceedings of the 28th International Conference on Artificial Intelligence and Statistics (AISTATS)},
  year      = {2025},
  note      = {arXiv:2305.11857v5; 本仓库 PDF 无会议页脚，会议信息取自仓库元数据},
  url       = {https://arxiv.org/abs/2305.11857}
}
```

---

**编者注（本报告作者自行决定的事项）**

1. **发表状态**：本仓库 PDF（arXiv v5, 2025-03-10）**没有任何会议页脚**，正文与附录也未提及 AISTATS。"AISTATS 2025 主会"取自 `data/papers.yaml` 的既有登记；报告与元数据均沿用该登记，但已在两处显式标注"PDF 未证实"。bibtex 中的 "28th" 会议届数是按 2025 年推断填写的，未由原文证实。
2. **术语澄清**被提到第 1 节末与第 6 节末各写一次（而非只在开头），因为这是本篇最容易被误读的点：标题里的 "flow neural network" = 连续正规化流 / 神经 ODE 速度场，**不是** GFlowNet。
3. 第 6 节第 6 条"摊销盈亏平衡点约 15–30 个任务"是本报告用 O05 的 35 小时与 O06 的 1.25–2.5 小时相除得到的粗估，两篇论文的硬件（H100 vs A100）与任务性质均不同，仅作数量级参考。
4. 第 4 节指出"未分离 OT 轨道与 flow-ratio 网络各自的贡献"是本报告的判断；原文只做了双向/单向（Table A.3）与 \(\gamma\)/网格（Table A.4）两组消融。
5. 阅读优先级沿用仓库登记的 P2，理由自拟（无结构共享，但作为"单对求解"这一端的参照物与 KL-松弛技巧来源有价值）。
6. Table 2 中 DiscoGAN / CycleGAN / NOT 的 FID 是原文从 Korotin et al. (2023) 抄录的，其余基线为原文自行在同一隐空间重训；报告已在正文说明，比较时不应混为一谈。
