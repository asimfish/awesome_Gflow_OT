# O04 · Schrödinger Bridge Flow for Unpaired Data Translation

> **一句话**：这篇把 DSBM 的"交替解两个投影子问题"换成"沿一条路径测度流做单步梯度下降"：定义路径测度上的流 \(\partial_s\hat{\mathbb{P}}_s=\mathrm{proj}_{\mathcal{R}}(\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}_s))-\hat{\mathbb{P}}_s\)，其唯一不动点就是 Schrödinger 桥；用步长 \(\alpha\in(0,1]\) 离散得到 \(\alpha\)-IMF（\(\alpha=1\) 恰好退化成原 IMF），参数化实现叫 \(\alpha\)-DSBM，一个网络、一套损失、在线微调。对 GFlowNet–OT 主线，它是**唯一一篇把"多条构造路径如何分配质量"写成两个显式投影算子、并给出算子层面收敛证明**的邻居工作：\(\mathrm{proj}_{\mathcal{R}}\) 就是"固定 \(P_B\)"、\(\mathrm{proj}_{\mathcal{M}}\) 就是"从边流反解 \(P_F\)"，两者的交替与 GFlowNet 训练的结构同源。

| 字段 | 内容 |
|---|---|
| arXiv | [2409.09347](https://arxiv.org/abs/2409.09347)（v1, 2024-09-14） |
| 发表 | **NeurIPS 2024 主会（Spotlight）**；本仓库 PDF 是 v1 预印本，页脚仍写 "Submitted to 38th Conference on NeurIPS 2024" |
| 作者 | Valentin De Bortoli\*、Iryna Korshunova\*（并列一作）、Andriy Mnih、Arnaud Doucet（均 Google DeepMind） |
| 代码 | **未公开**（NeurIPS Checklist 第 5 项：因 IP 限制无法公开代码库，计划发布小规模复现 notebook） |
| 本仓库 PDF | `papers/2409.09347.pdf`（47 MB，主要是图）· 中译 未生成 |
| 阅读优先级 | **P1** —— 它给出的两投影算子框架是把"GFlowNet 的 \(P_F/P_B\) 一致性训练"翻译成测度论语言的最好模板，且其失败模式（不是 simulation-free、必须自采样）直接对应 GFlowNet 的 on-policy 采样代价 |

## 1. 问题设定与记号

沿用原文记号。\(\pi_0,\pi_1\in\mathcal{P}(\mathbb{R}^d)\) 是两端（未配对）数据分布。\(\mathcal{P}(\mathcal{C})=\mathcal{P}(C([0,1],\mathbb{R}^d))\) 是路径测度空间，\(\mathcal{M}\) 是形如 \(\mathrm{d}X_t=v_t(X_t)\mathrm{d}t+\sigma_t\mathrm{d}B_t\) 的 Markov 路径测度子集。参考测度 \(\mathbb{Q}\) 由缩放布朗运动 \((\sqrt{\varepsilon}B_t)_{t\in[0,1]}\) 诱导。

**静态目标**是熵正则 OT（Eq. (1)）：

\[
\Pi^\star=\operatorname*{argmin}_{\Pi\in\mathcal{P}(\mathbb{R}^d\times\mathbb{R}^d)}\Big\{\int \tfrac12\|x-y\|^2\,\mathrm{d}\Pi(x,y)-\varepsilon H(\Pi)\ ;\ \Pi_0=\pi_0,\ \Pi_1=\pi_1\Big\}.
\]

**动态目标**是 Schrödinger 桥（Eq. (2)）：\(\mathbb{P}^\star=\operatorname*{argmin}_{\mathbb{P}}\{\mathrm{KL}(\mathbb{P}|\mathbb{Q});\ \mathbb{P}_0=\pi_0,\mathbb{P}_1=\pi_1\}\)。在温和条件下 \(\mathbb{P}^\star_{0,1}=\Pi^\star\)（引 Léonard 2014），所以解动态问题就解了静态问题。\(\varepsilon=0\) 退回标准 OT。

两个投影是全文的骨架：

- **Reciprocal 投影**（Def. 2.1）：\(\mathbb{P}\in\mathcal{R}(\mathbb{Q})\) 当且仅当 \(\mathbb{P}=\mathbb{P}_{0,1}\mathbb{Q}_{|0,1}\)，即"给定两端后的路径条件律"与布朗桥相同。\(\mathrm{proj}_{\mathcal{R}}(\mathbb{P})=\mathbb{P}_{0,1}\mathbb{Q}_{|0,1}\)。采样它**不需要训练网络**：抽 \((X_0,X_1)\sim\mathbb{P}_{0,1}\)，再按闭式 \(X_t=(1-t)X_0+tX_1+\sqrt{\varepsilon t(1-t)}Z\) 补中间点（Eq. (12)/(30)）。
- **Markov 投影**（Def. 2.2）：对 \(\mathbb{P}\in\mathcal{R}(\mathbb{Q})\)，\(\mathrm{proj}_{\mathcal{M}}(\mathbb{P})\) 是漂移为

\[
v_t^\star(x_t)=\big(\mathbb{E}_{\mathbb{P}_{1|t}}[X_1\mid X_t=x_t]-x_t\big)/(1-t)
\]

的扩散过程。它**必须回归**（训练网络）才能得到，且保边缘：\(\mathbb{M}_t=\mathbb{P}_t\ \forall t\)（Sec. 2 Def. 2.2 后一段）。

与标准 DAG-GFlowNet 设定的差异：

- **两端都固定**，且路径长度固定为时间区间 \([0,1]\)；GFlowNet 的轨迹长度 \(n_\tau\) 在非无环设定下无界（T19/T36/O07 的核心难点）。
- **参考测度显式给定**。\(\mathbb{Q}\) 是布朗运动，其桥有闭式；GFlowNet 里对应物是"参考 \(P_B\)"，通常取父节点上均匀分布，同样有闭式，但**图上"给定两端的参考随机游走条件律"一般没有闭式**（需要离散热核 / 逐端点对的归一化常数）。这是移植的第一道墙，原文自己在 Sec. 5 提到同类问题：Neklyudov 等、Liu 等（2022）把 DSBM 推广到一般代价，代价是 reciprocal 投影不再是布朗桥、必须学。
- **没有 reward、没有 \(Z\)**。这里"目标分布"是 \(\pi_1\)（有样本），不是未归一化 reward。
- **熵正则是必需品而非可选项**。\(\varepsilon\to0\) 时 DSBM 等价于 Rectified Flow（Appendix E.2 明说），而 \(\varepsilon>0\) 才有 SB 的唯一性刻画（Lemma D.2）。

## 2. 核心贡献（按原文编号）

**(C1) Schrödinger 桥流（Sec. 3.1, Eq. (3)）。** 结论：定义

\[
\hat{\mathbb{P}}_0=(\pi_0\otimes\pi_1)\mathbb{Q}_{|0,1},\qquad \partial_s\hat{\mathbb{P}}_s=\mathrm{proj}_{\mathcal{R}}(\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}_s))-\hat{\mathbb{P}}_s,\qquad \mathbb{P}_s=\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}_s),
\]

则该流的**唯一不动点是 SB**。论证只有两行（Sec. 3.1）：不动点 \(\bar{\mathbb{P}}\) 满足 \(\bar{\mathbb{P}}=\mathrm{proj}_{\mathcal{R}}(\mathrm{proj}_{\mathcal{M}}(\bar{\mathbb{P}}))\)，反复代入即知 \(\bar{\mathbb{P}}\) 是 IMF 序列的极限点，故为 SB（引 Peluchetti 2023, Thm. 2）。前提：流本身 well-defined（原文假设）。

**(C2) \(\alpha\)-IMF 与收敛定理（Eq. (4) + Theorem 3.1）。** \(\hat{\mathbb{P}}^{n+1}=(1-\alpha)\hat{\mathbb{P}}^{n}+\alpha\,\mathrm{proj}_{\mathcal{R}}(\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}^{n}))\)，\(\mathbb{P}^n=\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}^n)\)。对**任意** \(\alpha\in(0,1]\) 有 \(\lim_n\mathbb{P}^n=\mathbb{P}^\star\)；\(\alpha=1\) 恰好是 IMF/DSBM。注意 \(\hat{\mathbb{P}}^n\in\mathcal{R}(\mathbb{Q})\) 对所有 \(n\) 成立——凸组合保 reciprocal 类，这是整个构造能闭合的关键。

**(C3) 非参数更新 = \(\alpha\)-IMF（Prop. 3.2 / Prop. D.1）。** 取 \(\delta_n=\alpha\)、\(\mu_n=(1-\alpha)\hat{\mathbb{P}}^n+\alpha\,\mathrm{proj}_{\mathcal{R}}(\mathbb{P}^n)\)，则泛函梯度下降 \(v_t^{n+1}=v_t^{n}-\delta_n\nabla_{\mu_n}L_t(v_t^n,\mathbb{P}_{v^n})\) 产生的路径测度 \(\mathbb{P}_{v^n}\) 恰等于 \(\mathbb{P}^n\)。**这条是全文最有信息量的技术结论**（详见第 3 节）。

**(C4) 双向过程与单网络参数化（Sec. 4, Eq. (10)–(13), Prop. 4.1）。** 单向在线更新会累积误差（Appendix I 给出高斯情形的精确递推 Prop. I.1/I.2）；改成同时训前向与后向即可（因为 Markov 投影对前后向一致，引 Shi et al. 2023, Prop. 9）。参数化上不用两个网络，而是给网络加一个方向输入 \(s\in\{0,1\}\)，\(v_\theta(1,\cdot)\approx\overrightarrow{v}\)、\(v_\theta(0,\cdot)\approx\overleftarrow{v}\)。

**(C5) 参数更新是非参数更新的预条件版本（Prop. D.4 / D.5）。** \(\theta\leftarrow\theta-\alpha\nabla_\theta L(\theta,\mathbb{P}_{\bar\theta})\)（\(\bar\theta\) 为 stop-gradient）在 \(\alpha\to0\) 时平均意义上是非参数损失的下降方向。前提：Hessian 有界（Eq. (28) 的常数 \(C\)）。

**(C6) 与 Sinkhorn 流 / EM / RL 的三条平行关系（Appendix H）。** \(\gamma\)-Sinkhorn（Karimi et al. 2024）与 \(\gamma\)-IMF 的差别归结为一句话：**前者是隐式更新，后者是显式更新**（Table 2）。按 Brekelmans–Neklyudov 的分类，IPF 是 Expectation–Expectation、IMF 是 Maximisation–Maximisation，本文算法是 MM 的增量版（Appendix H.3）。Appendix H.2 给出 replay-buffer 版本（Algorithm 6），\(n_{\mathrm{refresh}}=1,\ N=B\) 时退化为 Algorithm 1。

## 3. 方法与理论推导要点

**(1) 桥匹配损失与它的泛函梯度（Eq. (5) + Eq. (22)）。** 损失是

\[
L(v,\mathbb{P})=\int_0^1\!\!\int_{(\mathbb{R}^d)^3}\Big\|v_t(x_t)-\frac{x_1-x_t}{1-t}\Big\|^2\,\mathrm{d}\mathbb{P}_{0,1}(x_0,x_1)\,\mathrm{d}\mathbb{Q}_{t|0,1}(x_t|x_0,x_1)\,\mathrm{d}t .
\]

其以 \(\mu\) 为参考测度的泛函梯度（Prop. D.1 证明中的 Eq. (22)）是

\[
\nabla_\mu L_t(v_t,\mathbb{P}_v)(x_t)=\Big(v_t(x_t)-\frac{\mathbb{E}_{\mathrm{proj}_{\mathcal{R}}(\mathbb{P})}[X_1|X_t=x_t]-x_t}{1-t}\Big)\cdot\frac{\mathrm{d}\,\mathrm{proj}_{\mathcal{R}}(\mathbb{P}_v)_t}{\mathrm{d}\mu_t}(x_t).
\]

括号里是"当前漂移与 Markov 投影目标漂移之差"，括号外是一个 **Radon–Nikodym 权重**。于是有效步长变成状态依赖的 \(\bar\delta^n_t(x_t)=\alpha\,\frac{\mathrm{d}\,\mathrm{proj}_{\mathcal{R}}(\mathbb{P}^n)_t}{\mathrm{d}((1-\alpha)\hat{\mathbb{P}}^n_t+\alpha\,\mathrm{proj}_{\mathcal{R}}(\mathbb{P}^n)_t)}(x_t)\)（Eq. (23)），且 \(1-\bar\delta^n_t(x_t)\) 正好是 \(\hat{\mathbb{P}}^n_t\) 对同一混合测度的密度（Eq. (24)）。代回后整条链塌缩成一句话：

\[
v^{n+1}_t(x_t)=\int_{\mathbb{R}^d}\frac{x_1-x_t}{1-t}\ \mathrm{d}\big[(1-\alpha)\hat{\mathbb{P}}^n+\alpha\,\mathrm{proj}_{\mathcal{R}}(\mathbb{P}^n)\big]_{1|t}(x_1|x_t).
\]

**为什么这一步是关键**：它说明"在损失上走一小步"与"把两个耦合做凸混合、再对终点取条件期望"是同一件事。也就是说 \(\alpha\) 不是学习率的工程近似，而是**耦合空间里的插值系数**。这正是把它与 GFlowNet 对照的接口（见第 6 节）。

**(2) 收敛证明的核心不等式（Prop. D.3, Eq. (26)）。** 用 KL 对第一个参数的凸性、数据处理不等式、以及 Markov 投影的勾股引理（Shi et al. 2023, Lemma 6）：

\[
\mathrm{KL}(\hat{\mathbb{P}}^{n+1}|\mathbb{P}^\star)\leqslant(1-\alpha)\mathrm{KL}(\hat{\mathbb{P}}^{n}|\mathbb{P}^\star)+\alpha\,\mathrm{KL}(\hat{\mathbb{P}}^{n}|\mathbb{P}^\star)-\alpha\,\mathrm{KL}(\hat{\mathbb{P}}^{n}|\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}^{n})),
\]

即 \(\alpha\,\mathrm{KL}(\hat{\mathbb{P}}^n|\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}^n))\leqslant\mathrm{KL}(\hat{\mathbb{P}}^n|\mathbb{P}^\star)-\mathrm{KL}(\hat{\mathbb{P}}^{n+1}|\mathbb{P}^\star)\)。求和后原文写作 \(\sum_n\mathrm{KL}(\hat{\mathbb{P}}^n|\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}^n))\leqslant 2\mathrm{KL}(\hat{\mathbb{P}}^0|\mathbb{P}^\star)<\infty\)，于是 \(\mathrm{KL}(\hat{\mathbb{P}}^n|\mathrm{proj}_{\mathcal{M}}(\hat{\mathbb{P}}^n))\to0\)：极限点同时是 Markov 的、又在 \(\mathcal{R}(\mathbb{Q})\) 里、且边缘正确，由 Lemma D.2（Léonard 2014, Thm. 2.12）唯一确定为 SB。**注意这只是收敛性，没有速率**；速率的信息只来自 Appendix B 的欧氏玩具模型。

**(3) 欧氏玩具模型给出的步长权衡（Appendix B）。** 取 \(A_1=\{y\geqslant x\}\)（类比 \(\mathcal{R}(\mathbb{Q})\)）、\(A_2=\{y\leqslant0\}\)（类比 \(\mathcal{M}\)），连续流以速率 \(1/2\) 指数收敛（\(x_t=x_0e^{-t/2}\)）；\(\alpha=1\) 的交替投影给出 \(x_n=x_02^{-n}\)；一般 \(\alpha\) 给出 \(x_n^\alpha=x_0^\alpha(1-\alpha/2)^n\)。达到精度 \(\epsilon\) 的总代价 \(C_n^\alpha=O\big(\log(1/\epsilon)\,C^\alpha/\log(1/(1-\alpha/2))\big)\)。**结论很反直觉但很诚实：若单步代价 \(C^\alpha\) 与 \(\alpha\) 无关，则 \(\alpha=1\)（即原 DSBM）最优**；\(\alpha<1\) 只在单步代价显著更低时才划算——而这正是实际情形（一次采样 + 一步梯度 vs 解一个完整的极小化子问题）。

**(4) 算法（Algorithm 1）。** 两阶段：先用 \((\pi_0\otimes\pi_1)\mathbb{Q}_{|0,1}\)（独立耦合 + 布朗桥）预训练一个 bridge matching 模型；再进入微调循环：抽 \((X_0,X_1)\sim(\pi_0\otimes\pi_1)^{\otimes b}\)，用当前模型解前向 SDE 得 \(\hat{X}_1\)、解后向 SDE 得 \(\hat{X}_0\)，把 \((X_0,\hat X_1)\) 与 \((\hat X_0,X_1)\) 分别插值出 \(X_t\)，对 Eq. (13) 的经验损失走**一步**梯度。EMA 参数既可用于采样也可只用于测试（原文给了两种消融）。实践中用 Adam，因此 \(\alpha\) 是隐式且自适应的（Sec. 4 末明说）——这是理论与实现之间一个诚实的缺口。

**(5) 损失预条件（Appendix J）。** 按 Karras et al. (2022) 的 EDM 思路推导 bridge matching 版本，得到闭式

\[
(c_t^i)^2=\big(1+(\varepsilon-2)t(1-t)\big)^{-1},\quad c_t^s=\frac{(\varepsilon-2)t-1}{1+(\varepsilon-2)t(1-t)},\quad (c_t^o)^2=\frac{1+t+(\varepsilon-2)t(1-t)}{1-t}.
\]

并注明：Karras 建议 \(\lambda_t=1/(c^o_t)^2\)，但本文实测取 \(\lambda_t=1\) 更好（有效权重因此是 \(1/(c^o_t)^2\)）。

**(6) 两个未进入正文实验的扩展，但对主线有用。**

- **反射–投影（Appendix E.4）**：把交替投影里的一个投影换成反射，\(\mathbb{P}^{n+1}=\mathrm{proj}_{\mathcal{R}(\mathbb{Q})}(2\mathbb{P}^{n+1/2}-\mathbb{P}^n)\)（Eq. (35)，引 Bauschke–Kruk 2004 说这类方案实践中收敛更快）。落到损失上，就是把 bridge matching 损失写成"新耦合上的损失减去 \(\alpha\) 倍旧耦合上的损失"（Eq. (37)/(38) 与 Algorithm 4 第 7–8 行）。原文明确给出参数对应：\(\alpha=1\) 回到 DSBM，\(\alpha=1/2\) 回到反射算法。**注意原文自己承认这里假设 \(2\mathbb{P}^{n+1/2}-\mathbb{P}^n\) 是概率测度，而其非负性并不清楚**——这是一个带已知漏洞的扩展，不能当定理引用。
- **一致性损失（Appendix F, Eq. (39)–(40)）**：对 \(\mathbb{P}\in\mathcal{R}(\mathbb{Q})\)，score 满足 \(\nabla\log p_t(x_t)=\int\{\nabla\log p_{t_0}+\nabla\log p_{t_1}\}\,\mathrm{d}p_{t_0,t_1|t}\)，又有 \(\overrightarrow{v}_t(x)+\overleftarrow{v}_{1-t}(x)=\sigma_0^2\nabla\log p_t(x)\)，于是可以对"前后向漂移之和在不同时刻的自一致性"加一项惩罚。这条对 GFlowNet 有直接类比：**"\(P_F\) 与 \(P_B\) 在不同子轨迹长度上的自一致性"就是 SubTB（T05）**，此处给出了它在连续扩散语言下的对应形式。

**(7) Markov 投影还有一个"最小误差"刻画（Appendix C, Prop. C.1–C.3）。** 给定一个随机插值 \(X_t=\alpha_tX_0+\beta_tX_1+\gamma_tZ\)，存在无穷多个与之同边缘的 Markov 过程（引 Albergo–Vanden-Eijnden 2023）。若把 \(\ell(\varepsilon)=\mathrm{KL}(\mathbb{P}^\varepsilon|\hat{\mathbb{P}}^\varepsilon)\) 当作"用近似 \(\hat Z(t,x)\) 替代 \(\mathbb{E}[Z|X_t]\) 所付的代价"，则最优噪声水平有闭式

\[
(\varepsilon^\star_t)^2=2\gamma_t\dot\gamma_t-2\gamma_t^2\dot\alpha_t/\alpha_t ,
\]

且当 \(\alpha_t=t,\beta_t=1-t,\gamma_t=\sigma_0\sqrt{t(1-t)}\) 时 \(\varepsilon_t=\sqrt2\sigma_0\)，**这个值恰好对应 Markov 投影**；Prop. C.3 进一步说明 \(\varepsilon^\star\) 与中间起点 \(s\) 无关。这给了"为什么选 Markov 投影而不是别的同边缘过程"一个非同义反复的理由：它是对漂移估计误差最不敏感的那一个。对 GFlowNet 的翻译是一个尚未被问过的问题：在"与给定状态访问频率相容"的诸多 \(P_F\) 中，哪一个对 \(\log F\) 的估计误差最鲁棒？

## 4. 实验与证据

| 任务 | 基线 | 指标 | 关键数字（出处） |
|---|---|---|---|
| 高斯（\(d=50,\sigma_0=\sigma_1=1,\varepsilon=0.5\)，闭式 EOT：\(\sigma_\star^2=\tfrac12((\sigma_0^2\sigma_1^2+\varepsilon^2)^{1/2}-\varepsilon)\approx0.88\)），对抗初始耦合 \(X_1=-X_0\) | 迭代 DSBM（每 1K/2.5K/5K 步换向） | 协方差收敛曲线 | 在线微调比迭代 DSBM 更快逼近 \(\sigma_\star^2\)；换向频率越高、迭代版行为越接近在线版（Sec. 6.1, Fig. 2）。**只有曲线，没有数值表** |
| EMNIST→MNIST（\(\varepsilon=1\)） | DSBM（Shi et al. 2023 原文数字）、自家重实现 | FID ↓ / MSD | DSBM\* 10.59 / 0.375；预训练两网络 6.02 / 0.564；(a) 迭代微调 **5.25±0.15** / 0.345±0.001；(b) 在线微调 **4.28±0.07** / 0.368±0.001；(c) 在线无 EMA 4.23±0.171 / 0.361±0.002；预训练双向 6.33 / 0.572；(d) 双向在线 4.39±0.09 / 0.387±0.003（Table 1，5 seeds） |
| AFHQ-64 Wild→Cat（\(\sqrt{\varepsilon}=0.75\)） | 同上 | FID ↓ / LPIPS | 预训练两网络 25.97 / 0.589；(a) 迭代 **25.41±0.84** / 0.485；(b) 在线 28.752±1.191 / 0.487；(c) 在线无 EMA 32.665±0.647 / **0.445**；(d) 双向在线 26.579±0.434 / 0.482（Table 1） |
| 2D 玩具（N→moons / scurve / 8gaussians、moons→8gaussians） | DSBM-IMF\*、OT-CFM\*（数字抄自 Shi et al. 2023） | 2-Wasserstein ↓ / path energy ↓ | \(\alpha\)-DSBM：0.168±0.011 / 0.213±0.031 / 0.292±0.047 / 1.374±0.286；DSBM-IMF\*：0.144 / 0.145 / 0.338 / 0.838；OT-CFM\*：0.111 / 0.102 / 0.253 / 0.716。path energy：\(\alpha\)-DSBM 1.439 vs DSBM 1.580 vs OT-CFM 1.178（N→moons）（Table 4） |
| AFHQ-256 | — | 视觉 | \(\sqrt{\varepsilon}=3.0\)（按 \(\varepsilon_{256}=\varepsilon_{64}(256/64)^2\) 的分辨率平移规则，Appendix K.4）；仅样例图 Fig. 24 |
| 单向 vs 双向的误差累积 | — | \(\|\mathbb{E}_{\mathbb{P}^{2n}}[X_1X_1^\top]-\mathrm{Id}\|\) | 高斯设定下 forward-forward 随 \(\varepsilon\)（Markov 投影误差）增大而爆炸，forward-backward 保持有界（Prop. I.1/I.2 + Fig. 8/9）；\(\varepsilon=0\) 时 \(\lim_n\mathbb{E}_{\mathbb{P}^{2n}}[X_1X_0^\top]=(\sqrt2-1)\mathrm{Id}\) |

规模与算力（Table 3 + Appendix K.3/K.4）：参数量 MNIST 8.8M、AFHQ-64 194.4M、AFHQ-256 226.7M；预训练 100K 步、微调 150K（MNIST）/ 20K（AFHQ）步。MNIST 双向预训练 1 小时（4×TPU v3）+ 在线微调 4 小时（16×TPU v3）；AFHQ-64 为 4h + 14h；AFHQ-256 为 15h + 37h。

证据强度分级（原文自己的措辞相当克制，这里照实转述）：

- **实验直接支持**：在 EMNIST↔MNIST 上在线微调的 FID 优于迭代微调（4.28 vs 5.25），且都优于 DSBM 原文的 10.59；在线版把可调超参从"内外迭代数、cache 刷新率、cache 大小"缩到基本只剩 \(\varepsilon\)（Sec. 6.2）。
- **实验不支持 / 相反**：AFHQ-64 上在线微调 FID **反而更差**（28.75 vs 迭代 25.41）；2D 玩具上 \(\alpha\)-DSBM 在 3/4 个任务上不如 DSBM-IMF 和 OT-CFM。原文对此的处理是明确劝退：AFHQ 测试集每域不到 500 张、FID 本不可靠（引 Chong & Forsyth 2020），且 2-Wasserstein 即使用 10K 样本也噪声极大（Fig. 11），建议重新设计 2D 实验协议。这是本文最值得学的写作态度，也意味着**"在线版更好"这个结论只在 MNIST 级别的证据上成立**。
- **作者推断**：\(\alpha<1\) 的优势来自单步代价更低（Appendix B 的成本模型），但没有在真实实验上做过"相同 wall-clock 下的对照"。
- **额外发现**：\(\varepsilon\) 控制视觉质量与对齐度的权衡（Fig. 3/16/17）；微调后用更少的采样步就能出清晰图，与 Rectified Flow 的观察一致（Fig. 18）；较大 \(\varepsilon\)（\(\sqrt\varepsilon=2.0\)）时模型对未见过的 AFHQ Dog 域泛化更好（Fig. 23）。

## 5. 前提假设与适用边界

1. **参考过程必须有闭式桥**。\(\mathrm{proj}_{\mathcal{R}}\) 免费的前提是 \(\mathbb{Q}_{|0,1}\) 为布朗桥（Eq. (12)/(30)）；换成一般代价，reciprocal 投影需要学（Sec. 5 引 Neklyudov 等、Liu 等 2022）。
2. **\(\varepsilon>0\) 固定且已知**，且 \(\pi_0,\pi_1\) 有二阶矩与有限微分熵（Lemma D.2 的显式条件：\(\int\|x\|^2\mathrm{d}\pi_i<\infty\)、\(H(\pi_i)<\infty\)）。
3. **必须能从模型自采样**。算法不是 simulation-free：每步微调都要解一次前/后向 SDE（Discussion 里列为主要局限）。
4. **双向训练是稳定性的必要条件**，不是可选优化项（Appendix I 的定量证据）。
5. **凸组合保持 reciprocal 类**是 \(\alpha\)-IMF 合法性的结构前提（Sec. 3.1）；如果 \(\mathcal{R}(\mathbb{Q})\) 不是凸集，整套构造失效。
6. **理论 \(\alpha\) 与实现 \(\alpha\) 不是同一个东西**。实现用 Adam，\(\alpha\) 隐式自适应（Sec. 4 末）；Prop. D.4/D.5 只保证 \(\alpha\to0\) 时的下降方向。

## 6. 在 GFlowNet × OT 主线中的位置

- **前驱**：DSBM/IMF（Shi et al. 2023；Peluchetti 2023）、bridge matching / stochastic interpolants（Peluchetti 2021；Albergo–Vanden-Eijnden 2023；Lipman et al. 2023）、DSB/IPF（De Bortoli et al. 2021）。OT 侧前驱是 Léonard 的 Schrödinger 问题综述与 Cuturi 的 Sinkhorn。本仓库内：O01（OT 基础）、O03（同为动态 OT，但用 PDE 残差）。
- **后继 / 竞争**：C02（Generalized Schrödinger Bridge on Graphs，预印本 2026）与 C03（Discrete Diffusion SB Matching for Graph Transformation，ICLR 2025）是把这套投影框架搬到离散/图状态空间的尝试——**这两篇才是 GFN–OT 的直接竞争者**，O04 是它们的方法论母本。
- **与主线（"内部流选择 = 最优传输"）的公式级对照**。把 GFlowNet 的对象与 O04 的对象逐一对齐：

  | GFlowNet（T02 / O08 记号） | O04（SB 记号） | 是否同构 |
  |---|---|---|
  | 轨迹测度 \(\mathbb{P}(\tau)=\prod_t P_F(s_{t+1}|s_t)\) | Markov 路径测度 \(\mathbb{P}\in\mathcal{M}\)，\(\mathrm{d}X_t=v_t\mathrm{d}t+\sqrt{\varepsilon}\mathrm{d}B_t\) | 同构：都是"由局部转移律生成的全局路径律" |
  | 固定参考 \(P_B(s|s')\) → 给定终点后的轨迹条件律确定 | \(\mathbb{P}=\mathbb{P}_{0,1}\mathbb{Q}_{|0,1}\)，即 \(\mathrm{proj}_{\mathcal{R}}\) | **同构**：\(\mathrm{proj}_{\mathcal{R}}\) 就是"把 \(P_B\) 拍回参考 \(P_B\)"，两者都只改中间、不改两端 |
  | 由边流反解前向策略 \(P_F(s'|s)=F(s\to s')/F(s)\) | \(v_t^\star(x_t)=(\mathbb{E}[X_1|X_t=x_t]-x_t)/(1-t)\)，即 \(\mathrm{proj}_{\mathcal{M}}\) | **同构**：都是"求与给定（非 Markov）轨迹测度同边缘的 Markov 策略"，且都保边缘（\(\mathbb{M}_t=\mathbb{P}_t\)） |
  | 训练目标：\(P_F\) 与 \(P_B\) 诱导的轨迹分布相同（O08 Eq. (1)） | 不动点条件：\(\bar{\mathbb{P}}=\mathrm{proj}_{\mathcal{R}}(\mathrm{proj}_{\mathcal{M}}(\bar{\mathbb{P}}))\)（Sec. 3.1） | 同构：都是"两个算子的公共不动点" |
  | 代价 = 期望轨迹长度 \(\mathbb{E}[n_\tau]\)（O07/O08） | 代价 = \(\int\tfrac12\|x-y\|^2\mathrm{d}\Pi-\varepsilon H(\Pi)\)（Eq. (1)） | **不同构**：图上代价来自"步数"，SB 的代价来自"位移平方 + 熵"；\(1-t\) 分母在图上无对应 |
  | 轨迹长度 \(n_\tau\) 无界（非无环） | 时间区间固定 \([0,1]\) | **不同构**，且这是移植的第二道墙 |
  | 熵/温度旋钮：策略熵、reward 指数 | \(\varepsilon\)（布朗运动方差） | 结构同源、量纲不同 |

- **"多条构造路径分配"的同与异，落到公式上**：
  - **同**：两边都在解"同一对端点之间有多条路径，质量怎么分"。GFlowNet 的答案是 \(\mathbb{P}(\tau)=\prod P_F=\prod P_B\)（O08 Eq. (1)）配合 reward matching \(P_B(x|s_f)=R(x)/Z\)（O08 Eq. (2)）；O04 的答案是 \(\mathbb{P}=\mathbb{P}_{0,1}\mathbb{Q}_{|0,1}\) 配合边缘约束 \(\mathbb{P}_0=\pi_0,\mathbb{P}_1=\pi_1\)。**两者都是"端点约束 + 给定端点后的路径律被参考测度钉死"这一模式的实例。**
  - **异（第一处，本质）**：GFlowNet 的路径分配是**组合的、离散的**，"多条路径"指图上不同的边序列；\(\mathrm{proj}_{\mathcal{R}}\) 的类比物是"给定 \((s_0\to u,\ x\to s_f)\) 后按参考随机游走重采中间状态"，而这在图上**没有闭式**（布朗桥的闭式插值 Eq. (12) 是欧氏结构的礼物）。C03 之所以要引入离散扩散，就是为了造一个可采样的离散桥。
  - **异（第二处，可利用）**：O04 的 \(\alpha\) 插值发生在**耦合空间**（Eq. (22) 塌缩式：新漂移 = 在 \((1-\alpha)\hat{\mathbb{P}}^n+\alpha\,\mathrm{proj}_{\mathcal{R}}(\mathbb{P}^n)\) 下对终点取条件期望）。GFlowNet 训练里对应的操作是"用旧策略与新目标的混合分布做 off-policy 更新"，也就是 replay buffer——O04 自己已经把这层关系写出来了（Appendix H.2, Algorithm 6）。**这意味着 GFlowNet 的 replay buffer 不只是工程技巧，它在 SB 语言下就是路径测度流的一种离散化。**
  - **异（第三处，方向性）**：GFlowNet 的 \(P_B\) 通常是**可学的**（或按 T02 的自由度被选择），而 O04 的 \(\mathbb{Q}_{|0,1}\) 是**钉死的**（正是"reciprocal class of \(\mathbb{Q}\)"的定义）。因此 O04 的解唯一（Lemma D.2），GFlowNet 在 \(P_B\) 自由时解不唯一——这个不唯一性恰恰是 O08 用"最小总流量"来消解的，消解后得到最短路支撑（O08 Theorem 3.3）。**换句话说：O04 用"固定参考桥 + 熵"选流，O08 用"最小总流量"选流；两者是同一件事的两种正则化。**

## 7. 可复用的 insight 与开放问题

1. **把 \(\alpha\)-IMF 直接翻译成"图上的 \(\alpha\)-IMF GFlowNet"。** 实验草案：在 O08 的 hypergrid 环境上，固定参考 \(P_B\) 为父节点均匀分布（充当 \(\mathbb{Q}_{|0,1}\)），交替执行 (i) 用当前 \(P_F\) 采样轨迹、只保留端点对 \((u,x)\)、按参考 \(P_B\) 重采中间路径（离散 \(\mathrm{proj}_{\mathcal{R}}\)）；(ii) 对重采后的轨迹做一步 TB/DB 梯度（离散 \(\mathrm{proj}_{\mathcal{M}}\)），步长 \(\alpha\)。度量：与 scipy LP 解的 \(\mathrm{OT}^\star\) 差距 vs 采样预算。假设 \(\alpha<1\) 在大图上更省，因为 (ii) 不需要训到收敛。
2. **一条定理草稿：图上 reciprocal 类的凸性与不动点唯一性。** O04 的合法性依赖两件事：\(\mathcal{R}(\mathbb{Q})\) 凸、且 "Markov + reciprocal + 边缘正确 ⟹ SB"（Lemma D.2）。在有限图上，"给定端点的参考游走条件律"定义的类同样是凸的（凸组合只改 \(\Pi_{0,1}\)）；需要补的是离散版 Léonard Thm. 2.12。若成立，则"最小总流量 GFlowNet + 熵正则"的唯一解就是图上的 Schrödinger 桥，且 \(\varepsilon\downarrow0\) 时应收敛到 O08 Theorem 3.3 的最短路支撑解。**这是本仓库最值得先做的理论题。**
3. **双向训练在 GFlowNet 上的对应物需要被显式检验。** O04 用 Prop. I.1/I.2 证明单向会累积误差、双向不会。GFlowNet 的"双向"天然存在（\(P_F\) 与 \(P_B\) 同时学），但很多实现把 \(P_B\) 固定。可做实验：固定 \(P_B\) vs 联合学 \(P_B\)，在长轨迹环境（如 O07 的 Rubik's Cube）里看 \(\mathbb{E}[n_\tau]\) 的偏差是否随迭代累积。
4. **"不是 simulation-free"是共享的成本瓶颈，值得共享解法。** O04 每步都要解 SDE，GFlowNet 每步都要 rollout 轨迹。O04 给出的两条缓解路线可以照搬：replay buffer（Appendix H.2）与 model stitching（Appendix G：用已有的两个生成模型初始化，取 \(\sigma_{t'}^2=\varepsilon/2\)，等价于在线版 DSBM-IPF）。对 GFlowNet 的翻译分别是"经验回放"与"从两个已训好的采样器拼接初始化"。
5. **\(\varepsilon\) 的分辨率平移规则是可迁移的工程结论。** \(\varepsilon_{256}=\varepsilon_{64}\cdot(256/64)^2\)（Appendix K.4）。图上的类比问题："状态空间规模从 \(n\) 增到 \(n'\) 时，熵正则系数该怎么缩放才能保持同样的 exploration"——这是一个可以直接做出来的标度律实验。
6. **评估协议要防止被噪声指标骗。** O04 主动指出 FID 在 <500 张测试集上不可靠、2-Wasserstein 在 10K 样本下仍高方差（Fig. 11）。GFN–OT 的评估若用"与精确 OT 解的传输代价差"，必须报多 seed 与置信区间，并在可解的小图上用 LP 做锚点（O08 已经这么做了）。
7. **一个尚未被利用的技术点：Radon–Nikodym 权重即"状态依赖步长"（Eq. (23)）。** 它说明理论上正确的更新对访问频率低的状态应该有更大的有效步长。GFlowNet 里存在同构问题（罕见状态的 credit assignment），可以据此设计一个显式的重要性权重项，并与 T05（SubTB）/T10 的方差分析对照。
8. **隐式 vs 显式更新这条区分值得搬到 GFlowNet 侧做消融。** Appendix H.1 的 Table 2 把 \(\gamma\)-Sinkhorn 与 \(\gamma\)-IMF 的差别定位为"隐式更新 vs 显式更新"（\(v^{n+1}_t=v^n_t-\delta\nabla_{\mu_n}L_t(v^{n+1},\bar{\mathbb{P}}^n)\) 对 \(v^{n+1}_t=v^n_t-\delta\nabla_{\mu_n}L_t(v^{n},\mathbb{P}^n)\)，Eq. (51) vs (52)）。GFlowNet 的 target network / stop-gradient 选择正是同一个轴上的选择，但据本报告所知尚无人把它当作"隐式–显式离散化"来分析。
9. **一个负面结论要记住，别浪费算力去复现"在线一定更好"。** AFHQ-64 上在线微调 FID 比迭代微调差（28.75 vs 25.41，Table 1），2D 玩具上 \(\alpha\)-DSBM 在 3/4 任务上不如两个基线（Table 4）。若 GFN–OT 里也做"在线 vs 分阶段"消融，应预期收益只出现在"内层子问题很贵"的大规模离散环境（这与 Appendix B 的成本模型一致），而不是小环境。

## 8. 引用

```bibtex
@inproceedings{debortoli2024sbflow,
  title     = {Schr{\"o}dinger Bridge Flow for Unpaired Data Translation},
  author    = {De Bortoli, Valentin and Korshunova, Iryna and Mnih, Andriy and Doucet, Arnaud},
  booktitle = {Advances in Neural Information Processing Systems (NeurIPS)},
  year      = {2024},
  note      = {Spotlight; arXiv:2409.09347v1},
  url       = {https://arxiv.org/abs/2409.09347}
}
```

---

**编者注（本报告作者自行决定的事项）**

1. **发表状态**：本仓库 PDF 是 arXiv v1，页脚仍为 "Submitted to 38th Conference on NeurIPS 2024"，未包含 camera-ready 的 Spotlight 标注；"NeurIPS 2024 Spotlight（主会）"这一判定取自 `data/papers.yaml` 的既有登记，报告中已把两处信息都列出，不做单一断言。
2. **代码**：NeurIPS Checklist 第 5 项明确因 IP 限制不公开代码库，故元数据 `code_url` 记为 `null`，正文写"未公开"。
3. Eq. (26) 求和后的常数原文写作 \(2\mathrm{KL}(\hat{\mathbb{P}}^0|\mathbb{P}^\star)\)（直接望远镜求和给出的是 \(\alpha^{-1}\mathrm{KL}(\hat{\mathbb{P}}^0|\mathbb{P}^\star)\)）。本报告照原文转述，不代替作者修正。
4. 第 6 节的 GFlowNet ↔ SB 对照表、以及"O04 用固定参考桥 + 熵选流 / O08 用最小总流量选流"这一判断，是本报告的分析，原文没有任何 GFlowNet 讨论。表中 GFlowNet 侧公式引自 O08（`papers/2606.06272.pdf`）Eq. (1)(2)(4) 与 Theorem 3.2/3.3。
5. Table 4（2D 玩具）的 DSBM-IMF 与 OT-CFM 数字是原文从 Shi et al. (2023) 抄录的，不是本文自己跑的，报告中已用 \* 标注。
6. 论文对 \(\alpha\) 的理论刻画与实现（Adam 自适应）之间存在缺口，本报告把它写进第 5 节而不是当作缺陷指控——原文自己在 Sec. 4 末已声明。
